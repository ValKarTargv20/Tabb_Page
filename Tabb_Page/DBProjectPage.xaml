<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Tabb_Page.DBProjectPage">
    <ContentPage.Content>
        <StackLayout Margin="10" Orientation="Vertical" VerticalOptions="Center">
            <Label Text="{Binding Name}" />
            <Image Source="{Binding Project_pic}" HeightRequest="70"/>
            <Label Text="{Binding Notes}"/>
            <Label x:Name="rows" Text="{Binding Rows}" />
            <Image Source="{Binding Pattern_pic}" HeightRequest="70"/>
            <StackLayout Orientation="Vertical" VerticalOptions="EndAndExpand" >
                <Button Text="+" Clicked="Plus_btn_Clicked" BackgroundColor="Cyan"/>
                <Button Text="-" Clicked="Minus_btn_Clicked" BackgroundColor="Cyan"/>
                <Button Text="Save Rows" Clicked="Save_rows_Clicked" BackgroundColor="Cyan"/>
                <Button Text="Delete Project" Clicked="Delete_btn_Clicked" BackgroundColor="Cyan"/>
                <Button Text="Back" Clicked="Cancel" BackgroundColor="Cyan"/>
            </StackLayout>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>